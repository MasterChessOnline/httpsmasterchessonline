import { Navigate } from "react-router-dom";

// Standalone Opening Trainer is consolidated into Master Courses.
const OpeningTrainer = () => <Navigate to="/learn" replace />;

export default OpeningTrainer;
